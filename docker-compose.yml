version: '3.9'
services:
  ssh.cyrhades.fr:
    build: ssh
  webssh.cyrhades.fr:
    build: webssh
    ports:
    - ${PORT}:8888
  proof-of-elapsed-time.cyrhades.fr:
    image: anssi/fcsc2025-hardware-proof-of-elapsed-time:latest
    ports:
      - ${PORT_SSH}:4000